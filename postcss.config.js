// 🔑 LEARNING: PostCSS config — PostCSS is the CSS processing pipeline.
// tailwindcss plugin scans your source files for class names and generates CSS.
// autoprefixer adds vendor prefixes (-webkit-, -moz-) for cross-browser compat.
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
